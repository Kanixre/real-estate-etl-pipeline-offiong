# real-estate-etl-pipeline-offiong
An end-to-end ETL pipeline that ingests real estate data from the RentCast API, enforces schema validation, cleans and enriches records, and loads analytics-ready datasets into PostgreSQL with full audit tracking.
